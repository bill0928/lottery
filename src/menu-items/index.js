import dashboard from './dashboard';
import module from './app';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, module]
};

export default menuItems;
